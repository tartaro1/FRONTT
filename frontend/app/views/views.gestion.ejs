<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>contenido</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-2">
                <nav class="navbar navbar-expand-lg bg-body-primary">
                    <div class="container-fluid flex-column menu">
                        <a class="navbar-brand mt-5" href="/dashboard">Tartaro</a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar">
                            <span class="navbar-toggler-icon"></span>
                        </button>
                        <div class="offcanvas offcanvas-start mt-5" tabindex="-1" id="offcanvasNavbar">
                            <div class="offcanvas-header">
                                <a class="navbar-brand" href="#">Tartaro</a>
                                <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                            </div>
                            <div class="offcanvas-body">
                                <ul class="navbar-nav flex-column justify-content-end flex-grow-1 pe-3">
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" aria-current="page" href="/dashboard/users"><i class="bi bi-person"></i> Usuarios</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/dashboard/products"><i class="bi bi-box2-heart"></i> Productos</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/dashboard/detailsOrders"><i class="bi bi-clipboard2-data"></i> Reportes</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/dashboard/orders"><i class="bi bi-backpack4"></i> Pedidos</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/dashboard/dealers"><i class="bi bi-bicycle"></i> Repartidores</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/dashboard/backup"><i class="bi bi-hdd-stack"></i> Copia de Seguridad</a>
                                    </li>
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/dashboard/gestion"><i class="bi bi-book-half"></i> Gestionar contenido</a>
                                    </li>
                                </ul>
                            </div>
                            <div class="offcanvas-footer mt-auto p-2" onclick="logOut()">
                                <h5 p-2><a class="nav-link text-dark m-3"><i class="bi bi-box-arrow-left"></i> Finalizar Sesion</a></h5>
                            </div>
                        </div>
                    </div>
                </nav>
            </div>
            <div class="col-sm-10">
                <div class="d-flex justify-content-between mb-3">
                    <div class="p-2"></div>
                    <div class="p-2">
                        <h1 class="text-center">Gestion de contenido</h1>
                    </div>
                    <div class="col-2 p-1 sidebar-item"><i class="sidebar-icon bi bi-person-circle"></i> <span style="margin-left: 10px;">Emmanuel</span></div>
                </div>
                <div class="row">
                    <div class="col-sm-5">
                        <form id="uploadForm" enctype="multipart/form-data">
                            <div class="mb-3">
                              <label for="titulo" class="form-label">Título</label>
                              <input type="text" class="form-control" id="titulo" name="titulo" required>
                            </div>
                            <div class="mb-3">
                              <label for="contenido" class="form-label">Contenido</label>
                              <textarea class="form-control" id="contenido" name="contenido" rows="3" required></textarea>
                            </div>
                            <div class="mb-3">
                              <label for="formFile" class="form-label">Archivo PDF</label>
                              <input type="file" class="btn btn-primary form-control form-control-lg" id="formFile" name="pdf">
                            </div>
                            <button type="submit" class="btn btn-primary">Subir</button>
                          </form>
                    </div>
                    <div class="col-sm-7">
                        <h2>Archivos Subidos</h2>
                        <ul id="uploadedFiles"></ul>
                    </div>

                </div>
            </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('uploadForm').addEventListener('submit', async (event) => {
    event.preventDefault();
    const formData = new FormData(event.target);
    const response = await fetch('https://ms-backend-tartaro.onrender.com/gestion', {
        method: 'POST',
        body: formData,
    });
    if (response.ok) {
        alert('Contenido subido exitosamente');
        const result = await response.json();
        const pdfPath = result.data.pdf_path;
        console.log(pdfPath);
        const uploadedFilesList = document.getElementById('uploadedFiles');
        const listItem = document.createElement('li');
        const downloadLink = document.createElement('a');
        downloadLink.href = `https://ms-backend-tartaro.onrender.com${pdfPath}`;
        downloadLink.innerText = 'Descargar PDF';
        downloadLink.setAttribute('download', '');
        downloadLink.download = `https://ms-backend-tartaro.onrender.com${pdfPath}`
        listItem.appendChild(downloadLink);
        uploadedFilesList.appendChild(listItem);
    } else {
        alert('Error subiendo contenido');
    }
});

    </script>
</body>
</html>
